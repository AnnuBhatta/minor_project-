Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.0
channels==4.0.0
daphne==4.1.2
channels-redis==4.2.0
django-allauth==0.57.0
djangorestframework-dataclasses==1.3.1
pyrebase4==4.5.0
firebase-admin==6.4.0
geopy==2.4.1
django-filter==23.5
drf-yasg==1.21.7
# PostgreSQL driver for production. Only needed when DATABASE_URL points at
# PostgreSQL (Docker uses it automatically; local SQLite dev does not need it).
psycopg2-binary==2.9.9
python-dotenv==1.0.0
requests==2.31.0
joblib==1.4.2
numpy==1.26.4

# Additional packages
scikit-learn==1.6.1        # pinned to match the version the .joblib models were trained/retrained with
pandas==2.2.3               # 2.2.x supports numpy 2.x (required by tensorflow 2.21)
tensorflow==2.21.0          # Keras LSTM risk model (lstm_risk_model.keras)
redis==5.0.1                # Redis client for Channels
django-redis==5.4.0         # Django Redis cache backend
dj-database-url==3.1.2      # Env-driven DATABASE_URL for production (PostgreSQL)
django-ckeditor==6.7.0      # Rich text editor (optional)
django-import-export==3.3.6 # Import/export data (optional)
